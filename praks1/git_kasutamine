* root kasutajana
installi - apt-get install git
* tavakasutajana:
esmakordselt:

a) seadista - maara kasutajanimi, email, editor
git config --global user.name "JereminFreed Meister"
git config --global user.email j.meister@khk.ee
git config --global core.editor nano

b) loo kataloog kus hakkad skriptima
mkdir skriptimine

c)sisene antud kausta
cd skriptimine

d)tekita lokaalne repositoorium git systeemi jaoks antud kaustas
git init

remote seadistamine:

a) git remote add nimi aadress
git remote add skriptimine https://github.com/JereminFreedMeister/reponimi.git

b) git remote kontroll
git remote -v
